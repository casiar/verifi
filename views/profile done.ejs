<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Public Profile</title>
    <link href="/public/css/styles.css" rel="stylesheet">
</head>
<body class="bg-blue-100">
    <div class="container mx-auto p-4 min-h-screen flex flex-col justify-center items-center">
        <h1 class="text-2xl font-bold mb-4 text-blue-800">Public Profile</h1>
        <% if (user) { %>
            <div class="relative">
                <img src="<%= user.avatar %>" alt="User Avatar" class="w-32 h-32 object-cover rounded-full mb-4">
                <img src="/images/logo.png" alt="Logo" class="absolute top-0 right-0 h-8 w-8">
                <% let angle = 0; %>
                <% let radius = 60; %>
                <% user.connectedAccounts.forEach((account) => { %>
                    <img src="<%= account.icon %>" alt="<%= account.provider %> Icon" class="absolute w-16 h-16 object-cover rounded-full border-2 border-blue-800 bg-white" style="transform: translate(-50%, -50%) rotate(<%= angle %>deg) translateY(-<%= radius %>px);">
                    <% angle += 360 / user.connectedAccounts.length; %>
                <% }) %>
            </div>
            <div class="bg-white p-4 rounded shadow mt-8">
                <h2 class="text-xl font-semibold mb-2 text-blue-800"><%= user.displayName %></h2>
                <p>Provider: <%= user.provider %></p>
            </div>
        <% } else { %>
            <p>No user found.</p>
        <% } %>
    </div>
</body>
</html>
